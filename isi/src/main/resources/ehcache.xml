<?xml version="1.0" encoding="UTF-8"?>

<ehcache xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="ehcache.xsd" updateCheck="false" monitoring="autodetect" dynamicConfig="true">

    <diskStore path="java.io.tmpdir"/>

	<!-- Don't know what this is ... -->
    <transactionManagerLookup class="net.sf.ehcache.transaction.manager.DefaultTransactionManagerLookup" properties="jndiName=java:/TransactionManager" propertySeparator=";"/>
	
    <defaultCache maxElementsInMemory="1000" eternal="false" timeToIdleSeconds="120" timeToLiveSeconds="120" memoryStoreEvictionPolicy="LRU" statistics="true" />

	<!-- Hibernate's Cache for keeping 'lastUpdated' data on each table.  Should never expire. -->
	<cache name="org.hibernate.cache.UpdateTimestampsCache" eternal="true" maxElementsInMemory="1000" statistics="true" />
		
	<!-- Hibernate's Query Cache - should probably be limited -->
	<cache name="org.hibernate.cache.StandardQueryCache" maxElementsInMemory="1000" statistics="true" />
	
	<!-- Transformed Content -->
    <cache name="org.cast.cwm.xml.DomCache" maxElementsInMemory="500" memoryStoreEvictionPolicy="LFU" statistics="true" />

</ehcache>